# Migrations

Quy tắc đặt tên:

```text
YYYYMMDD_HHMM_short_description.sql
```

Ví dụ:

```text
20260428_001_add_auto_arrange_metadata.sql
```

Không sửa trực tiếp file `Dump20260428.sql` sau khi đã đưa vào repo, trừ khi cả nhóm chốt refresh lại dump gốc.
